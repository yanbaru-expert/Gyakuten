<h1>テキスト教材画像アップロード</h1>
<%= form_with model: @text_image, local: true do |form| %>
  <div class="text-center mb-3">
    <img class="image-preview d-none" alt="プレビュー画像" width="200">
  </div>
  <div class="custom-file mb-3">
    <%= form.file_field :image, class: "custom-file-input image-field", required: true, accept: "image/png,image/jpeg,image/gif" %>
    <%= form.label :image, class: "custom-file-label", data: { browse: "参照" } %>
  </div>
  <div class="form-group mb-4">
    <%= form.label :title %>
    <%= form.text_field :title, class: "form-control" %>
    <small>画像のalt属性になります。必須ではありません</small>
  </div>
  <div>
    <%= form.submit "送信", class: "btn btn-primary btn-block" %>
  </div>
<% end %>
<hr>
<h2 class="text-center">投稿一覧</h2>
<div class="bt-5 text-center">
  <%= render @text_images %>
</div>
<%= paginate @text_images %>
