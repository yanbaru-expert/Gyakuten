<object><%= link_to "読破済みを解除", progress_path(materiable_id, materiable_type), class: "btn btn-primary btn-block", method: :delete, data: { remote: true } %></object>

<%# <object><%= link_to "読破済みを解除", text_read_texts_path(text_id), class: "btn btn-primary btn-block", method: :delete, data: { remote: true } %></object> %>
