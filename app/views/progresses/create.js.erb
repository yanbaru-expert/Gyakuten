document.getElementById('materiable-<%= @materiable.id %>').innerHTML = '<%= j render("progresses/completed", materiable: @materiable) %>'
$readModal = $('#read-modal')
if($readModal.length) {
  $readModal.modal({keyboard: false}).show()
}
